{
  "application_credentials": {
    "description": "Nothing to add: the integration signs in through its own published Cloudflare OAuth client.\n\nAdd a credential only to use a client of your own; it then replaces the built-in one. Create it under Manage Account → OAuth clients with redirect URL `{redirect_uri}`, grant types authorization code and refresh token, token endpoint authentication none (PKCE) and the scopes {scopes}; enter its client ID with an empty secret.\n\n[Cloudflare OAuth clients]({docs_oauth_clients}) · [project notes]({more_info_url})"
  },
  "config": {
    "step": {
      "user": {
        "title": "Cloudflare Access",
        "description": "Puts the whole hostname behind Cloudflare Access: only people with a Home Assistant account get in, and voice assistants or MCP clients sign in through Access.\n\nContinue to sign in with Cloudflare; the consent page lists the permissions used.\n\nUse a desktop browser: the Android companion app cannot finish the return from the consent page."
      },
      "pick_implementation": {
        "title": "Sign in with Cloudflare",
        "data": {
          "implementation": "Credentials"
        }
      },
      "account": {
        "title": "Cloudflare account",
        "description": "The sign-in was granted for several accounts. Choose the one whose zone serves the hostname.",
        "data": {
          "account_id": "Account"
        }
      },
      "api_token": {
        "title": "Cloudflare API token",
        "description": "An account-level token with the permissions 'Access: Apps and Policies: Edit' and 'Access: Organizations, Identity Providers, and Groups: Read'. Stored in the config entry.",
        "data": {
          "api_token": "API token",
          "account_id": "Account ID"
        }
      },
      "settings": {
        "title": "Hostname and options",
        "description": "The gate starts off; nothing changes at the edge until you switch it on in the options.\n\nWho gets in: every active user whose login username is an e-mail address, or whose external login stored one. Currently: {allowed_users}.\n\nHome Assistant has no e-mail field of its own: [change the username]({docs_change_username}) to the address, or use a [login provider]({docs_auth_providers}) that stores one.",
        "data": {
          "hostname": "Hostname",
          "extra_bypass_paths": "Paths open without Access",
          "client_redirect_uris": "Redirect URLs of self-registering clients",
          "service_token_ids": "Service tokens allowed through",
          "session_duration": "Session duration",
          "delete_objects_on_remove": "Delete the Access applications when the integration is removed"
        },
        "data_description": {
          "hostname": "The public hostname Cloudflare serves, e.g. ha.example.com.",
          "extra_bypass_paths": "Path prefixes reachable without a login, for callers that hold neither cookie nor token.\n\nPick from what this Home Assistant serves, or type one. [Bypass policies]({docs_policies})",
          "client_redirect_uris": "Where self-registering clients (MCP) may be sent back to; nothing else can register.\n\nExample: {example_mcp_redirect}, or {example_wildcard_redirect} for every path. [Managed OAuth]({docs_managed_oauth})\n\nA client that asks for a client ID and secret: use \"Add OAuth client\" instead.",
          "service_token_ids": "Client IDs of Access service tokens that may pass the gate, e.g. for a script on another machine. [Service tokens]({docs_service_tokens})",
          "session_duration": "How long a login lasts before Access asks again; also a registered client's refresh-token lifetime. [Session duration]({docs_session})",
          "delete_objects_on_remove": "Off keeps the Access applications in Cloudflare after removal, e.g. to adopt them from another instance."
        }
      },
      "reauth_confirm": {
        "title": "Sign in with Cloudflare again",
        "description": "Cloudflare no longer accepts the sign-in. Continue to sign in again."
      }
    },
    "error": {
      "invalid_auth": "Cloudflare rejected the credential. It needs 'Access: Apps and Policies: Edit' on this account.",
      "missing_org_read": "The credential cannot read the Zero Trust organization (the team domain). It needs 'Access: Organizations, Identity Providers, and Groups: Read'.",
      "cannot_connect": "Cloudflare could not be reached.",
      "api_error": "Cloudflare returned an error; see the log.",
      "invalid_hostname": "Enter a hostname such as ha.example.com.",
      "invalid_redirect_uri": "Redirect URLs must start with https://.",
      "invalid_duration": "The session must last at least one minute."
    },
    "abort": {
      "already_configured": "This hostname is already configured.",
      "reauth_successful": "The credential was replaced.",
      "invalid_auth": "Cloudflare rejected the credential. It needs 'Access: Apps and Policies: Edit' on this account.",
      "missing_org_read": "The credential cannot read the Zero Trust organization (the team domain). It needs 'Access: Organizations, Identity Providers, and Groups: Read'.",
      "cannot_connect": "Cloudflare could not be reached.",
      "api_error": "Cloudflare returned an error; see the log.",
      "no_accounts": "The sign-in reaches no Cloudflare account with a Zero Trust organization. Select the account on Cloudflare's consent page, and set up Zero Trust in it first."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Cloudflare Access for {hostname}",
        "description": "Saving re-provisions the Access applications; reloading the integration does the same without a change.\n\nWho gets in: every active user whose login username is an e-mail address, or whose external login stored one. Currently: {allowed_users}.\n\nHome Assistant has no e-mail field of its own: [change the username]({docs_change_username}) to the address, or use a [login provider]({docs_auth_providers}) that stores one.",
        "data": {
          "gate_enabled": "Gate the whole hostname",
          "extra_bypass_paths": "Paths open without Access",
          "client_redirect_uris": "Redirect URLs of self-registering clients",
          "service_token_ids": "Service tokens allowed through",
          "session_duration": "Session duration",
          "delete_objects_on_remove": "Delete the Access applications when the integration is removed"
        },
        "data_description": {
          "gate_enabled": "On: every request needs an Access login, except the open paths.\nOff: nothing is gated.",
          "extra_bypass_paths": "Path prefixes reachable without a login, for callers that hold neither cookie nor token.\n\nPick from what this Home Assistant serves, or type one. [Bypass policies]({docs_policies})",
          "client_redirect_uris": "Where self-registering clients (MCP) may be sent back to; nothing else can register.\n\nExample: {example_mcp_redirect}, or {example_wildcard_redirect} for every path. [Managed OAuth]({docs_managed_oauth})\n\nA client that asks for a client ID and secret: use \"Add OAuth client\" instead.",
          "service_token_ids": "Client IDs of Access service tokens that may pass the gate, e.g. for a script on another machine. [Service tokens]({docs_service_tokens})",
          "session_duration": "How long a login lasts before Access asks again; also a registered client's refresh-token lifetime. [Session duration]({docs_session})",
          "delete_objects_on_remove": "Off keeps the Access applications in Cloudflare after removal, e.g. to adopt them from another instance."
        }
      }
    },
    "error": {
      "no_allowed_users": "No user has an e-mail address as login username, so the gate would let nobody in. See above for how to give one.",
      "invalid_redirect_uri": "Redirect URLs must start with https://.",
      "invalid_duration": "The session must last at least one minute."
    }
  },
  "issues": {
    "restart_required": {
      "title": "Restart Home Assistant to accept clients authenticated by Cloudflare Access",
      "description": "The integration was set up after the web server had started, so its authentication middleware could not be installed. Until Home Assistant is restarted, requests from token-bearing clients (Google Assistant, Alexa, MCP clients) that Cloudflare Access admitted are rejected by Home Assistant."
    },
    "no_allowed_users": {
      "title": "No Home Assistant user can pass the Cloudflare Access gate",
      "description": "No active user has an e-mail address as login username or from an external login, so there is nobody to list in the gate's allow policy; it was left as it was.\n\n[Change a username]({docs_change_username}) to the address, or use a [login provider]({docs_auth_providers}) that stores one, then reload the integration."
    }
  },
  "config_subentries": {
    "oauth_client": {
      "entry_type": "OAuth client",
      "initiate_flow": {
        "user": "Add OAuth client"
      },
      "step": {
        "user": {
          "title": "Register an OAuth client",
          "description": "For a client whose console asks for a client ID and secret: Google Home account linking, the Alexa skill, any service that cannot register itself.\n\nAccess issues the credentials; the next page shows them with the URLs to enter in that console. Who may link is the gate's own allow rule.",
          "data": {
            "name": "Name",
            "redirect_uris": "Redirect URLs"
          },
          "data_description": {
            "name": "Anything that identifies the client to you, e.g. Google Home.",
            "redirect_uris": "The callback URL(s) the client's console shows, e.g. {example_console_redirect}."
          }
        },
        "reconfigure": {
          "title": "Change the OAuth client",
          "description": "Saving updates the registration in Access and shows the credentials again.",
          "data": {
            "name": "Name",
            "redirect_uris": "Redirect URLs"
          }
        },
        "credentials": {
          "title": "Credentials for {name}",
          "description": "Enter these in the client's console. The secret is shown only here and on reconfiguration.\n\nClient ID: `{client_id}`\n\nClient secret: `{client_secret}`\n\nAuthorization URL: `{authorization_url}`\n\nToken URL: `{token_url}`\n\nUser info URL: `{userinfo_url}`\n\nScopes: `openid email profile`"
        }
      },
      "error": {
        "required": "This field is required.",
        "invalid_redirect_uri": "Redirect URLs must start with https://.",
        "invalid_auth": "Cloudflare rejected the credential.",
        "cannot_connect": "Cloudflare could not be reached.",
        "api_error": "Cloudflare returned an error; see the log."
      },
      "abort": {
        "reconfigure_successful": "The client was updated."
      }
    }
  }
}
