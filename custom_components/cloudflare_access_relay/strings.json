{
  "config": {
    "step": {
      "user": {
        "title": "Cloudflare Access relay",
        "description": "The integration creates and maintains two Access applications for the hostname: a gate (allow policy for the people listed, and Access's own OAuth server for token-bearing clients) and a bypass (login, static assets and the relay endpoints). The gate starts disabled: only the relay callback path is protected until you enable it in the options.",
        "data": {
          "api_token": "Cloudflare API token",
          "account_id": "Cloudflare account ID",
          "hostname": "Hostname",
          "allowed_emails": "Allowed e-mail addresses",
          "access_group_id": "Access group ID (instead of e-mails)",
          "extra_bypass_paths": "Extra bypassed paths",
          "service_token_ids": "Service token IDs allowed through the gate",
          "session_duration": "Access session duration",
          "cookie_name": "Cookie name",
          "identity_claim": "Identity claim",
          "user_match": "Home Assistant user field",
          "renew_days": "Renew when fewer days remain",
          "check_interval_min": "Check interval",
          "delete_objects_on_remove": "Delete the Access applications when the integration is removed",
          "client_redirect_uris": "Redirect URIs allowed for self-registering clients"
        },
        "data_description": {
          "api_token": "Account-level token with the permissions 'Access: Apps and Policies: Edit' and 'Access: Organizations, Identity Providers, and Groups: Read'. Stored in the config entry.",
          "hostname": "The public hostname served through Cloudflare, e.g. ha.example.com.",
          "allowed_emails": "Identity-provider e-mail addresses the gate lets in. One per line.",
          "extra_bypass_paths": "Hostname-relative path prefixes that server-to-server callers reach without a cookie, e.g. /api/webhook/abc.",
          "service_token_ids": "IDs of Access service tokens (Zero Trust → Access → Service auth) that may pass the gate with CF-Access-Client-Id/Secret headers, as a Service Auth policy. Used by the live tests; safer than bypassing a path.",
          "session_duration": "Lifetime of the Access token, e.g. 720h. Cloudflare's ceiling is one month.",
          "identity_claim": "JWT claim compared with the Home Assistant user, usually email.",
          "user_match": "Which Home Assistant user field must equal the claim: 'username' (built-in login), 'name' (display name), or any credential field such as 'email' for OIDC providers.",
          "client_redirect_uris": "Clients that discover the OAuth server themselves (MCP clients such as Claude) register with Access dynamically; Access accepts their registration only if their redirect URI is listed here. https:// URLs, optionally ending in /* for all sub-paths. Clients with a console that asks for a client id and secret are registered instead under this entry's \"Add OAuth client\"."
        }
      },
      "reauth_confirm": {
        "title": "Replace the Cloudflare API token",
        "data": {
          "api_token": "Cloudflare API token"
        }
      }
    },
    "error": {
      "invalid_auth": "Cloudflare rejected the token. It needs 'Access: Apps and Policies: Edit' on this account.",
      "missing_org_read": "The token cannot read the Zero Trust organization (the team domain). Add the permission 'Access: Organizations, Identity Providers, and Groups: Read' to it.",
      "cannot_connect": "Cloudflare could not be reached.",
      "api_error": "Cloudflare returned an error; see the log.",
      "invalid_hostname": "Enter a hostname such as ha.example.com.",
      "no_policy_subject": "List at least one e-mail address or an Access group ID.",
      "invalid_email": "One of the e-mail addresses is not valid.",
      "required": "This field is required.",
      "invalid_redirect_uri": "Redirect URIs must start with https://."
    },
    "abort": {
      "already_configured": "This hostname is already configured.",
      "reauth_successful": "The token was replaced."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Cloudflare Access relay for {hostname}",
        "description": "Changing an option re-provisions the Access applications (reload the integration to re-provision without a change). Enabling the gate is the exposure change: the whole hostname then requires an Access session, except the bypassed paths.",
        "data": {
          "gate_enabled": "Gate the whole hostname",
          "allowed_emails": "Allowed e-mail addresses",
          "access_group_id": "Access group ID (instead of e-mails)",
          "extra_bypass_paths": "Extra bypassed paths",
          "service_token_ids": "Service token IDs allowed through the gate",
          "session_duration": "Access session duration",
          "cookie_name": "Cookie name",
          "identity_claim": "Identity claim",
          "user_match": "Home Assistant user field",
          "renew_days": "Renew when fewer days remain",
          "check_interval_min": "Check interval",
          "delete_objects_on_remove": "Delete the Access applications when the integration is removed",
          "client_redirect_uris": "Redirect URIs allowed for self-registering clients"
        },
        "data_description": {
          "client_redirect_uris": "Clients that discover the OAuth server themselves (MCP clients such as Claude) register with Access dynamically; Access accepts their registration only if their redirect URI is listed here. https:// URLs, optionally ending in /* for all sub-paths. Clients with a console that asks for a client id and secret are registered instead under this entry's \"Add OAuth client\"."
        }
      }
    },
    "error": {
      "no_policy_subject": "List at least one e-mail address or an Access group ID.",
      "invalid_email": "One of the e-mail addresses is not valid.",
      "required": "This field is required.",
      "invalid_redirect_uri": "Redirect URIs must start with https://."
    }
  },
  "issues": {
    "restart_required": {
      "title": "Restart Home Assistant to accept clients authenticated by Cloudflare Access",
      "description": "The integration was set up after the web server had started, so its authentication middleware could not be installed. Until Home Assistant is restarted, requests from token-bearing clients (Google Assistant, Alexa, MCP clients) that Cloudflare Access admitted are rejected by Home Assistant."
    }
  },
  "config_subentries": {
    "oauth_client": {
      "entry_type": "OAuth client",
      "initiate_flow": {
        "user": "Add OAuth client"
      },
      "step": {
        "user": {
          "title": "Register an OAuth client",
          "description": "For a client whose console asks for a client id and secret (Google Home account linking, the Alexa skill, any service that cannot register itself). Access issues the credentials; the next page shows them together with the authorization and token URLs to enter in that console. Who may link the client is the gate's own allow rule.",
          "data": {
            "name": "Name",
            "redirect_uris": "Redirect URIs"
          },
          "data_description": {
            "name": "Anything that identifies the client to you, e.g. Google Home.",
            "redirect_uris": "The callback URL(s) the client's console shows. Exact https:// URLs."
          }
        },
        "reconfigure": {
          "title": "Change the OAuth client",
          "description": "Saving updates the registration in Access and shows the credentials again.",
          "data": {
            "name": "Name",
            "redirect_uris": "Redirect URIs"
          }
        },
        "credentials": {
          "title": "Credentials for {name}",
          "description": "Enter these in the client's console. The secret is shown only here and on reconfiguration.\n\nClient ID: `{client_id}`\n\nClient secret: `{client_secret}`\n\nAuthorization URL: `{authorization_url}`\n\nToken URL: `{token_url}`\n\nUser info URL: `{userinfo_url}`\n\nScopes: `openid email profile`"
        }
      },
      "error": {
        "required": "This field is required.",
        "invalid_redirect_uri": "Redirect URIs must start with https://.",
        "invalid_auth": "Cloudflare rejected the API token.",
        "cannot_connect": "Cloudflare could not be reached.",
        "api_error": "Cloudflare returned an error; see the log."
      },
      "abort": {
        "reconfigure_successful": "The client was updated."
      }
    }
  }
}
